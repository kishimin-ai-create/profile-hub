// Components move here once two applications genuinely need the same one, and
// once the design token set is settled. Abstracting a shared component library
// before either is true would fix decisions the design work has not made yet.
export {};
